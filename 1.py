import turtle
turtle.speed(0)
turtle.bgcolor("black")
for i in range (91) :
    for colours in ("red", "magenta", "blue", "cyan", "green", "yellow", "white") :
        turtle.color(colours)
        turtle.pensize(4)
        turtle.left(0)
        turtle.forward(200)
        turtle.left(120)
        turtle.forward(200)
        turtle.left(120)
        turtle.forward(200)
        turtle.left(123)
